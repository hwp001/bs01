<scroll-view class="menu" scroll-y>
    <block wx:for="{{categories}}" wx:key="{{index}}">
        <view class="item {{currentIndex == index ? 'active' : ''}}" data-index="{{index}}" bind:tap="onItemClick">
            {{item.title}}
        </view>
    </block>
</scroll-view>